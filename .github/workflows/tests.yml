name: SCART CI DIAG

on:
  workflow_dispatch:

jobs:
  diag:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hello
        shell: pwsh
        run: |
          Write-Host "Workflow dispatch OK"
          python --version
          pip --version
